# Copy to .env and fill in real values. Never commit .env — it's git-ignored.

# @BotFather → /newbot → the token it gives you
TELEGRAM_BOT_TOKEN=

# console.anthropic.com → API Keys
ANTHROPIC_API_KEY=
